import { Component } from '@angular/core';

// PUBLIC_INTERFACE
@Component({
  selector: 'app-root',
  template: `<app-dashboard></app-dashboard>`
})
/** AppComponent: Root wrapper of the application main module */
export class AppComponent {}
